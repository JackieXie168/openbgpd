#ifndef PBGP_SCHEMA_H
#define PBGP_SCHEMA_H

typedef enum {
  SETUP = 1,
  SETUP_ELEMENT,
  IBE,
  JOIN,
  ASLIST,
  CIDR,
  EPOCH,
} store_namespace_t ;

typedef enum {
  SETUP_G = 1,
  SETUP_IBEPUB,
  SETUP_Z,
  SETUP_N,
  SETUP_RSA_PK,
  SETUP_RSA_SK,
  SETUP_GAMMA,
  SETUP_GAMMAPOW_NP1,

  IBE_PRIV0,
  IBE_PRIV1,
  IBE_PUB0,
  IBE_PUB1,

  MESSAGE,

  JOIN_ASNUM,
  JOIN_WITNESS,
  JOIN_ENVELOPE_SIGNATURE,
  JOIN_CIDR_SIGNATURE,

  EPOCH_ACCUMULATOR,
  EPOCH_VALUE,
  EPOCH_SIGNATURE_ADDED,
  EPOCH_SIGNATURE_REVOKED,
  EPOCH_SIGNATURE_ACCUMULATOR,
  EPOCH_REVOKED,
  EPOCH_ADDED,

  GLOBAL_ADDED,
  GLOBAL_REVOKED,
} store_key_index_t ;

#endif
